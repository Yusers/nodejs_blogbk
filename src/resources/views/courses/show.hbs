<div class="mt-4">
    <div class="row">
        <div class="col-lg-3">
            <button>Học Ngay</button>
        </div>
        <div class="col-lg-9">
            <h2>{{course.name}}</h2>
            <image class="card" src="{{course.image}}"></image>
            <p>{{course.description}}</p>
        </div>
    </div>
</div>